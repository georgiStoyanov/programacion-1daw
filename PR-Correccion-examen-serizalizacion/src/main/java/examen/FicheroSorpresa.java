package examen;

public class FicheroSorpresa {

    /**
     * Recibirá un nombre de fichero. Se garantiza que el fichero existe.
     * El fichero puede ser:
     * - Un fichero de texto UTF-8: se devolverá el contenido del fichero como String
     * - Un fichero con un objeto serializado: se devolverá el valor como cadena del objeto, con String.valueOf()
     * - Cualquiera de los anteriores, comprimido con GZIP: se descomprimirá y se devolverá lo mismo que si no estuviera comprimido
     * 
     * 
     * @param file
     * @return
     */
    public String ficheroSorpresa( String file ){
        return null;
    }
}
